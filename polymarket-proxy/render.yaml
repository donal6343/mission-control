services:
  - type: web
    name: polymarket-proxy
    runtime: node
    region: frankfurt
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PROXY_SECRET
        generateValue: true
